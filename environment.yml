name: key2med
channels:
  - nodefaults
  - conda-forge
  - pytorch
  # - fastai
dependencies:
  - python>=3.8
  - pip
  # BASICS
  - pip:
      - -r requirements.txt
      - -e .  # install git checkout of key2med in editable mode
